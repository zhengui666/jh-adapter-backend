{
  "name": "jh-adapter-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Jihu CodeRider OpenAI Proxy - Cloudflare Workers deployment",
  "type": "module",
  "scripts": {
    "build": "echo 'No build step required for Cloudflare Workers'",
    "deploy": "npm install && wrangler deploy",
    "deploy:cloudflare": "npm install && wrangler deploy"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "wrangler": "^4.55.0",
    "typescript": "^5.6.3"
  }
}

